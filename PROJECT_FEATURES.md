# 宣传教育平台 - 业务功能点文档

## 文档信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 宣传教育平台 (Propaganda/Education Platform) |
| 文档版本 | v1.0 |
| 生成日期 | 2026-01-26 |
| 项目定位 | 企业级素材资产管理系统，支持素材管理、审批流转、使用追踪 |

---

## 目录

- [项目概述](#项目概述)
- [用户角色和权限](#用户角色和权限)
- [核心业务功能模块](#核心业务功能模块)
  - [素材管理](#素材管理)
  - [审批流程管理](#审批流程管理)
  - [使用申请管理](#使用申请管理)
  - [任务管理](#任务管理)
  - [用户管理](#用户管理)
  - [部门管理](#部门管理)
  - [角色管理](#角色管理)
  - [菜单管理](#菜单管理)
  - [标签管理](#标签管理)
  - [日志管理](#日志管理)
- [工作流引擎功能](#工作流引擎功能)
- [系统配置](#系统配置)

---

## 项目概述

### 产品定位

宣传教育平台是一个企业级的素材资产管理系统，旨在解决企业内部素材的统一管理、审批流转和使用追踪问题。系统支持素材的全生命周期管理，包括上传、审批、使用和追踪。

### 核心价值

- **统一管理**: 集中管理企业所有素材资产
- **规范审批**: 灵活可配置的工作流引擎，支持复杂审批场景
- **权限控制**: 基于角色的多级权限管理，支持数据隔离
- **使用追踪**: 完整记录素材使用情况，便于审计和统计
- **提高效率**: 简化审批流程，提高协作效率

### 技术架构

- **后端**: Java Spring Boot + COLA架构 + MyBatis Plus + MySQL
- **前端**: Vue 3 + TypeScript + Vite + Element Plus
- **认证**: JWT Token 认证
- **工作流**: 自研工作流引擎，支持层级顺序+层内并行

---

## 用户角色和权限

### 角色类型

系统支持以下角色类型，通过 `sys_role` 表的 `role_type` 字段定义：

| 角色类型 | 角色代码 | 说明 | 权限范围 |
|---------|---------|------|---------|
| 系统管理员 | SYSTEM_ADMIN | 拥有系统所有权限 | 所有功能和数据 |
| 总消保管理岗 | GENERAL_MGMT | 可管理部门用户，配置角色权限 | 所有部门和用户 |
| 分消保管理岗 | BRANCH_MGMT | 可管理二级机构的用户 | 所属二级机构及其下级部门 |
| 总消保用户 | GENERAL_USER | 总消保部普通用户 | 基础功能权限 |
| 分消保用户 | BRANCH_USER | 分消保部普通用户 | 基础功能权限 |
| 普通用户 | CUSTOM | 自定义角色 | 根据分配的菜单权限 |

### 部门层级

系统采用三级部门结构：

```
一级：总公司 (level=1)
  ├─ 二级：总部门 (level=2)
  │   └─ 三级：总消保部 (level=3)
  └─ 二级：分部门 (level=2)
      └─ 三级：分消保部 (level=3)
```

### 权限控制机制

1. **菜单权限**: 基于角色的菜单访问控制（RBAC）
2. **数据权限**: 分消保管理岗只能查看和管理所属二级机构的数据
3. **角色分配权限**:
   - 系统管理员和总消保管理岗：可分配所有角色
   - 分消保管理岗：不能分配系统管理员和总消保管理岗角色

---

## 核心业务功能模块

## 素材管理

### 功能概述

提供素材的全生命周期管理，支持素材的上传、查看、审批、使用等环节。

### 素材类型

| 类型 | 代码 | 说明 | 支持格式 |
|------|------|------|----------|
| 视频 | VIDEO | 各类视频文件 | mp4, avi, mov 等 |
| 图片 | IMAGE | 各类图片文件 | jpg, png, gif 等 |
| 文档 | DOCUMENT | 各类文档文件 | pdf, doc, docx 等 |

### 素材状态

| 状态 | 代码 | 说明 | 转换条件 |
|------|------|------|----------|
| 草稿 | DRAFT | 素材刚上传，等待提交 | 上传素材 |
| 待审 | PENDING | 素材进入审批流程 | 提交审批 |
| 可用 | APPROVED | 素材审批通过，可正常使用 | 审批通过 |
| 驳回 | REJECTED | 审批被驳回 | 审批驳回 |
| 已退回 | RETURNED | 审批退回上一阶段 | 退回操作 |

### 核心功能

#### 1. 素材上传

**功能说明**: 支持上传视频、图片、文档三类素材

**特性**:
- 支持最大 100MB 单文件上传
- MD5 去重检测，避免重复存储
- 自动生成缩略图（图片）
- 记录上传者信息和时间
- 支持保存为草稿或直接提交审批

**API**: `POST /asset/upload`

**参数**:
- `file`: 文件对象
- `name`: 素材名称
- `type`: 素材类型（VIDEO/IMAGE/DOCUMENT）
- `copyright`: 版权信息（可选）
- `status`: 状态（DRAFT/PENDING）

#### 2. 素材列表查询

**功能说明**: 分页查询素材列表，支持多条件筛选

**筛选条件**:
- 素材名称（模糊搜索）
- 素材类型（视频/图片/文档）
- 素材状态（草稿/待审/可用/驳回）
- 角色权限过滤（分消保管理岗只看所属部门数据）

**视图模式**:
- 列表模式：表格展示详细信息
- 预览模式：图片缩略图网格展示

**API**: `GET /asset/list`

**返回字段**:
- 素材基本信息（ID、名称、类型、状态）
- 上传者信息
- 创建时间
- 缩略图路径

#### 3. 素材预览

**功能说明**: 在线预览素材内容

**支持格式**:
- 图片：直接展示
- 视频：在线播放
- 文档：嵌入式预览

**API**: `GET /asset/preview/{id}`

#### 4. 素材下载

**功能说明**: 下载素材文件到本地

**权限控制**:
- 所有用户（包括上传者）都需要通过使用审批才能下载
- 检查用户是否有该素材的使用权限
- 下载后自动记录使用日志

**使用权限检查**:
- 检查是否有已通过的使用申请
- 通过中间表 `usage_apply_asset` 查询权限

**API**: `GET /asset/download/{id}`

**记录信息**:
- 使用人
- 部门
- IP 地址
- 使用说明
- 发布渠道
- 使用时间

#### 5. 素材删除

**功能说明**: 删除无用素材

**删除规则**:
- 只能删除自己上传的素材
- 已被使用的素材不能删除（逻辑删除）
- 已有使用申请的素材不能删除

**API**: `DELETE /asset/{id}`

#### 6. 素材使用详情

**功能说明**: 查看素材的使用记录

**展示信息**:
- 使用次数统计
- 使用记录列表（分页）
- 使用人、部门、使用说明、发布渠道、使用时间

**API**: 内嵌在素材预览弹窗中

### 数据模型

**数据库表**: `asset`

**核心字段**:
```sql
- id: 素材ID
- name: 素材名称
- type: 素材类型（VIDEO/IMAGE/DOCUMENT）
- file_path: 文件存储路径
- thumbnail_path: 缩略图路径
- file_size: 文件大小
- md5: 文件MD5值（唯一索引）
- status: 状态（DRAFT/PENDING/APPROVED/REJECTED/RETURNED）
- copyright: 版权信息
- upload_user_id: 上传者ID
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

---

## 审批流程管理

### 功能概述

提供灵活可配置的审批工作流引擎，支持可视化流程设计。系统采用**"层级顺序 + 层内并行"**的标准化模型。

### 流程类型

| 流程类型 | 代码 | 说明 | 应用场景 |
|---------|------|------|----------|
| 素材录入审批 | ASSET_UPLOAD | 素材上传后的审批流程 | 素材录入 |
| 素材使用审批 | ASSET_USAGE | 素材使用申请的审批流程 | 素材使用申请 |

### 核心概念

#### 1. 流程架构模型

**层级顺序（层间）**: 流程必须完成第 N 层的全部审批逻辑，方可进入第 N+1 层

**并行处理（层内）**: 进入某一层后，该层内所有审批人同时收到待办任务

**层级自动跳过**: 若某层级无有效审批人，系统自动进入下一层

#### 2. 审批层 (Stage)

流程的最小流转阶段，包含：

**核心配置项**:
- 层名称
- 层顺序（stage_order）
- 层内规则（会签/或签）
- 审批人配置

#### 3. 层内规则

| 规则类型 | 代码 | 说明 | 通过条件 |
|---------|------|------|----------|
| 或签 | OR | 任一人审批通过即通过 | 一人通过即可 |
| 会签 | AND | 所有人都必须审批通过 | 所有人通过 |

#### 4. 审批人类型

| 审批人类型 | 代码 | 说明 |
|-----------|------|------|
| 指定用户 | USER | 指定具体某个用户 |
| 指定角色 | ROLE | 该角色下的所有用户 |
| 指定部门 | DEPT | 该部门下的所有用户 |
| 发起人自己 | INITIATOR | 由发起人确认 |

### 核心功能

#### 1. 流程列表

**功能说明**: 查看所有审批流程

**展示信息**:
- 流程名称、描述
- 流程类型（素材录入/素材使用）
- 绑定角色
- 版本号
- 状态（启用/禁用）

**筛选条件**:
- 流程类型

**API**: `GET /workflow/list`

#### 2. 流程创建/编辑

**功能说明**: 创建新的审批流程或编辑现有流程

**可视化设计器**:
- 拖拽式设计审批层级轨道
- 支持层级卡片排序
- 可视化配置流转逻辑

**配置项**:
- 流程名称
- 流程描述
- 流程类型
- 审批层级配置
- 每层审批人配置
- 层内规则配置

**API**:
- `POST /workflow` - 创建流程
- `PUT /workflow` - 更新流程

#### 3. 流程复制

**功能说明**: 快速复制已有流程创建新流程

**特性**:
- 复制流程基本信息
- 复制所有审批层级配置
- 复制审批人配置
- 自动生成新流程名称

**API**: `POST /workflow/{id}/copy`

#### 4. 流程启用/禁用

**功能说明**: 控制流程的可用状态

**API**: `PUT /workflow/{id}/status`

#### 5. 流程删除

**功能说明**: 删除未使用的流程

**删除规则**:
- 只有无关联实例的流程才能删除
- 有实例关联的流程需要先禁用

**API**: `DELETE /workflow/{id}`

#### 6. 角色绑定

**功能说明**: 将流程绑定到特定角色，实现角色级流程配置

**应用场景**:
- 不同角色使用不同的审批流程
- 根据用户角色自动选择审批流程

**API**:
- `PUT /workflow/{id}/bind-role` - 绑定角色
- `PUT /workflow/{id}/unbind-role` - 解除绑定

**参数**:
- `roleId`: 角色ID
- `workflowType`: 流程类型（ASSET_UPLOAD/ASSET_USAGE）

### 子流程功能

#### 子流程配置

**配置位置**: 在审批人级别配置（`stage_approver` 表）

**触发方式**: 由第一个审批人手动触发

**执行特点**:
- 独立运行，不阻塞主流程
- 与主流程并行执行
- 需要主流程和所有子流程都完成才算最终完成

**应用场景**:
- 需要额外部门会签
- 需要专家评审
- 需要外部意见

### 数据模型

**数据库表**:
- `workflow` - 流程定义表
- `workflow_stage` - 审批阶段表
- `stage_approver` - 阶段审批人配置表

**核心字段**:

**workflow**:
```sql
- id: 流程ID
- name: 流程名称
- description: 流程描述
- workflow_type: 流程类型（ASSET_UPLOAD/ASSET_USAGE）
- bound_role_id: 绑定角色ID
- version: 版本号
- status: 状态（0-禁用，1-启用）
```

**workflow_stage**:
```sql
- id: 阶段ID
- workflow_id: 流程ID
- name: 阶段名称
- stage_order: 阶段顺序
- approve_type: 审批类型（OR-或签，AND-会签）
- sub_workflow_id: 子流程ID（可选）
```

**stage_approver**:
```sql
- id: 审批人配置ID
- stage_id: 阶段ID
- approver_type: 审批人类型（USER/ROLE/DEPT/INITIATOR）
- approver_id: 审批人ID
- sub_workflow_id: 子流程ID（可选）
- check_secondary_dept: 是否检查二级部门
```

---

## 使用申请管理

### 功能概述

管理用户对素材的使用申请，支持一个申请包含多个素材的多对多关系。

### 申请状态

| 状态 | 代码 | 说明 | 转换条件 |
|------|------|------|----------|
| 草稿 | DRAFT | 申请单创建但未提交 | 创建申请 |
| 待审 | PENDING | 申请进入审批流程 | 提交申请 |
| 已批准 | APPROVED | 申请审批通过 | 审批通过 |
| 已驳回 | REJECTED | 申请审批被驳回 | 审批驳回 |
| 已退回 | RETURNED | 申请退回上一阶段 | 退回操作 |

### 核心功能

#### 1. 创建使用申请

**功能说明**: 创建素材使用申请草稿

**特性**:
- 支持单素材申请（旧API兼容）
- 支持多素材申请（新API）
- 每个素材可单独配置使用信息
- 保存为草稿，可后续编辑提交

**多素材配置**:
- 使用说明（usage_description）
- 发布渠道（usage_publish_channel）
- 是否二次创作（usage_is_secondary_creation）
- 附件路径（usage_attachment_path）

**API**:
- `POST /usage-apply/apply` - 旧API（单素材）
- `POST /usage-apply/draft` - 新API（草稿）

**参数**:
- `title`: 申请标题
- `assetIds`: 素材ID列表（多素材）
- `assetsConfig`: 素材配置列表（每素材的配置信息）

#### 2. 更新使用申请

**功能说明**: 更新草稿状态的使用申请

**API**: `PUT /usage-apply/{id}`

#### 3. 提交使用申请

**功能说明**: 提交使用申请进入审批流程

**流程**:
1. 选择审批流程（系统根据角色自动推荐）
2. 选择第一阶段审批人（如果流程需要）
3. 如果有子流程，选择子流程审批人
4. 创建审批实例
5. 发送审批任务给审批人

**API**: `POST /usage-apply/{id}/submit`

**参数**:
- `workflowId`: 审批流程ID
- `firstStageApprovers`: 第一阶段审批人列表（可选）
- `subWorkflowApprovers`: 子流程审批人配置（可选）

#### 4. 查询我的申请

**功能说明**: 查看当前用户的所有使用申请

**筛选条件**:
- 申请标题（模糊搜索）
- 申请状态

**API**: `GET /usage-apply/my`

#### 5. 查询草稿箱

**功能说明**: 查看所有草稿状态的申请

**特性**:
- 支持按类型筛选（素材录入/素材使用）
- 支持按标题搜索
- 按创建时间倒序排序

**API**: `GET /usage-apply/drafts`

#### 6. 删除使用申请

**功能说明**: 删除草稿状态的申请

**删除规则**:
- 只能删除草稿状态的申请
- 已提交的申请不能删除

**API**: `DELETE /usage-apply/{id}`

#### 7. 复制申请单

**功能说明**: 基于已有申请创建新申请草稿

**特性**:
- 复制基本信息
- 复制素材配置
- 保留草稿状态

**API**: `POST /usage-apply/{id}/copy`

#### 8. 权限检查

**功能说明**: 检查用户是否有权限使用某素材

**检查逻辑**:
- 查询该用户对该素材的已通过申请
- 通过中间表 `usage_apply_asset` 查询
- 返回是否有权限

**API**: `GET /usage-apply/can-use/{assetId}`

### 数据模型

**数据库表**:
- `usage_apply` - 使用申请主表
- `usage_apply_asset` - 使用申请素材关联表（中间表）

**核心字段**:

**usage_apply**:
```sql
- id: 申请ID
- title: 申请标题
- user_id: 申请人ID
- status: 状态（DRAFT/PENDING/APPROVED/REJECTED/RETURNED）
- workflow_id: 使用的审批流程ID
- instance_id: 审批实例ID（提交后生成）
- create_time: 创建时间
- update_time: 更新时间
```

**usage_apply_asset**（中间表）:
```sql
- id: 关联ID
- usage_apply_id: 使用申请ID
- asset_id: 素材ID
- usage_description: 使用说明
- usage_publish_channel: 发布渠道
- usage_is_secondary_creation: 是否二次创作
- usage_attachment_path: 附件路径
```

**关系**:
- 一个使用申请可以包含多个素材
- 一个素材可以被多个使用申请包含

---

## 素材录入管理

### 功能概述

管理素材录入申请，用户上传素材后需要经过审批才能正式入库。

### 申请状态

| 状态 | 代码 | 说明 | 转换条件 |
|------|------|------|----------|
| 草稿 | DRAFT | 素材已上传但未提交 | 上传素材 |
| 待审 | PENDING | 进入审批流程 | 提交申请 |
| 已批准 | APPROVED | 审批通过，素材正式入库 | 审批通过 |
| 已驳回 | REJECTED | 审批被驳回 | 审批驳回 |
| 已退回 | RETURNED | 退回上一阶段 | 退回操作 |

### 核心功能

#### 1. 创建素材录入申请

**功能说明**: 上传素材并创建录入申请

**流程**:
1. 上传素材文件
2. 填写素材信息（名称、类型、版权等）
3. 保存为草稿或直接提交

**API**: `POST /material-application`

**参数**:
- `assetId`: 素材ID（已上传的素材）
- `title`: 申请标题
- `description`: 申请描述

#### 2. 提交素材录入申请

**功能说明**: 提交素材录入申请进入审批流程

**流程**:
1. 选择审批流程（素材录入类型）
2. 选择第一阶段审批人
3. 创建审批实例
4. 发送审批任务

**API**: `POST /material-application/{id}/submit`

**参数**:
- `workflowId`: 审批流程ID
- `firstStageApprovers`: 第一阶段审批人列表

#### 3. 查询我的素材录入申请

**功能说明**: 查看当前用户的所有素材录入申请

**API**: `GET /material-application/my`

#### 4. 复制素材录入申请

**功能说明**: 基于已有申请创建新申请

**特性**:
- 复制申请信息
- 关联到原素材
- 保存为草稿

**API**: `POST /material-application/{id}/copy`

### 数据模型

**数据库表**: `material_application`

**核心字段**:
```sql
- id: 申请ID
- asset_id: 素材ID
- title: 申请标题
- description: 申请描述
- user_id: 申请人ID
- status: 状态（DRAFT/PENDING/APPROVED/REJECTED/RETURNED）
- workflow_id: 使用的审批流程ID
- instance_id: 审批实例ID
- create_time: 创建时间
- update_time: 更新时间
```

---

## 任务管理

### 功能概述

统一管理用户的各类任务，包括待审批任务、我发起的申请、草稿箱等。

### 任务类型

| 任务类型 | 说明 | 来源 |
|---------|------|------|
| 待我审批 | 需要当前用户审批的任务 | 审批实例 |
| 我发起的 | 当前用户发起的所有申请 | 素材录入/使用申请 |
| 草稿箱 | 当前用户创建的草稿 | 素材录入/使用申请 |
- 素材使用申请（ASSET_USAGE）

### 核心功能

#### 1. 待我审批

**功能说明**: 查看需要当前用户审批的任务

**展示信息**:
- 申请类型（素材录入/素材使用）
- 申请标题
- 申请人信息
- 申请时间
- 当前阶段
- 审批状态

**操作**:
- 查看详情
- 审批通过
- 审批驳回
- 退回上一级
- 查看审批进度

**API**: `GET /approval/tasks`

**参数**:
- `pageNum`: 页码
- `pageSize`: 每页大小

#### 2. 我发起的

**功能说明**: 查看当前用户发起的所有申请

**展示信息**:
- 申请类型
- 申请标题
- 申请时间
- 当前状态
- 当前阶段
- 可执行操作

**筛选条件**:
- 业务类型（素材录入/素材使用）
- 申请人
- 部门
- 角色类型
- 状态
- 是否查看所有用户（管理员权限）

**操作**:
- 查看详情
- 查看审批进度
- 追回工单（发起人）
- 重新发起子流程

**API**: `GET /approval/applied`

#### 3. 草稿箱

**功能说明**: 查看和管理当前用户的所有草稿

**展示信息**:
- 草稿类型（素材录入/素材使用）
- 标题
- 创建时间

**筛选条件**:
- 草稿类型
- 标题（模糊搜索）

**操作**:
- 继续编辑
- 提交审批
- 删除草稿

**API**: `GET /task/drafts`

**特点**:
- 统一展示素材录入和使用申请草稿
- 按创建时间倒序排序
- 分页展示

---

## 审批管理

### 功能概述

处理审批任务，包括审批、驳回、退回、追回等操作。

### 审批操作

#### 1. 审批通过

**功能说明**: 审批人同意当前申请

**流程**:
1. 检查是否为第一个审批人
2. 如果是第一个审批人，需要选择下一阶段审批人
3. 如果有子流程，触发子流程
4. 更新任务状态
5. 检查阶段是否完成
6. 如果完成，进入下一阶段
7. 如果是最后阶段，完成审批

**API**: `POST /approval/tasks/{id}/approve`

**参数**:
- `comment`: 审批意见
- `passed`: 是否通过（true/false）
- `nextStageApprovers`: 下一阶段审批人（首审必填）
- `subWorkflowApprovers`: 子流程审批人（可选）

**第一审批人特殊逻辑**:
- 或签：第一个审批的人
- 会签：任务创建时标记的人
- 需要选择下一阶段审批人
- 可以触发子流程

#### 2. 审批驳回

**功能说明**: 审批人拒绝当前申请

**流程**:
1. 更新任务状态为已驳回
2. 更新实例状态为已驳回
3. 取消其他待审批任务
4. 通知发起人

**API**: `POST /approval/tasks/{id}/approve` (passed=false)

#### 3. 退回上一级

**功能说明**: 将申请退回到上一审批阶段

**流程**:
1. 检查是否有上一阶段
2. 取消当前阶段其他任务
3. 重新创建上一阶段任务
4. 更新实例状态
5. 通知上一阶段审批人

**API**: `POST /approval/tasks/{id}/return`

**参数**:
- `comment`: 退回意见

#### 4. 追回工单

**功能说明**: 发起人追回正在审批的工单

**适用场景**:
- 发起人发现信息有误需要修改
- 发起人想要撤销申请

**流程**:
1. 检查是否为发起人
2. 检查实例状态（必须是 PENDING 或 MAIN_COMPLETED）
3. 取消所有待审批任务
4. 更新实例状态
5. 通知相关审批人

**API**: `POST /approval/instances/{id}/withdraw`

**参数**:
- `comment`: 追回原因

#### 5. 重新发起子流程

**功能说明**: 子流程失败后重新发起

**适用场景**:
- 子流程被驳回
- 子流程审批人配置错误

**API**: `POST /approval/tasks/{id}/restart-sub-workflow`

**参数**:
- `approverIds`: 新的审批人ID列表

### 审批进度

**功能说明**: 查看审批实例的完整进度

**展示信息**:
- 审批流程图
- 各阶段审批状态
- 各审批人审批结果
- 审批意见
- 审批时间
- 子流程进度

**API**: `GET /approval/instances/{id}/detail`

### 数据模型

**数据库表**:
- `approval_instance` - 审批实例表
- `approval_task` - 审批任务表
- `approval_progress` - 审批进度记录表

**核心字段**:

**approval_instance**:
```sql
- id: 实例ID
- workflow_id: 流程ID
- business_type: 业务类型（MATERIAL_ENTRY/ASSET_USAGE）
- business_id: 业务ID
- applicant_id: 申请人ID
- current_stage_id: 当前阶段ID
- status: 状态（PENDING/MAIN_COMPLETED/APPROVED/REJECTED）
- parent_instance_id: 父实例ID（子流程）
- parent_task_id: 父任务ID（子流程）
- sub_workflow_approver_ids: 子流程审批人ID列表（JSON）
```

**approval_task**:
```sql
- id: 任务ID
- instance_id: 实例ID
- stage_id: 阶段ID
- approver_id: 审批人ID
- status: 状态（PENDING/APPROVED/REJECTED/RETURNED/CANCELLED）
- comment: 审批意见
- approve_time: 审批时间
- is_first_approver: 是否首审批人
- next_stage_approver_ids: 下一阶段审批人ID列表（JSON）
- sub_workflow_approver_ids: 子流程审批人ID列表（JSON）
```

---

## 用户管理

### 功能概述

管理系统用户，包括用户的创建、编辑、删除和查询。

### 核心功能

#### 1. 用户列表

**功能说明**: 查询系统用户列表

**权限控制**:
- 系统管理员和总消保管理岗：查看所有用户
- 分消保管理岗：只查看所属二级机构的用户

**筛选条件**:
- 角色筛选（多选）
- 部门筛选
- 是否包含子部门

**API**: `GET /user/listWithFilter`

#### 2. 创建用户

**功能说明**: 创建新用户

**权限控制**:
- 系统管理员和总消保管理岗：可创建所有部门用户
- 分消保管理岗：只能创建所属二级机构的用户

**必填字段**:
- 用户名（唯一）
- 密码
- 真实姓名
- 部门
- 角色

**API**: `POST /user`

#### 3. 编辑用户

**功能说明**: 更新用户信息

**权限控制**:
- 同创建用户的权限规则
- 只能编辑有权限管理的部门的用户

**API**: `PUT /user`

#### 4. 删除用户

**功能说明**: 删除用户（逻辑删除）

**权限控制**:
- 同创建用户的权限规则
- 只能删除有权限管理的部门的用户

**API**: `DELETE /user/{id}`

#### 5. 获取当前用户

**功能说明**: 获取当前登录用户信息

**API**: `GET /user/current`

#### 6. 默认筛选部门

**功能说明**: 获取当前用户的默认筛选部门配置

**应用场景**:
- 分消保管理岗默认筛选其二级机构
- 限制分消保管理岗的部门选择范围

**API**: `GET /user/defaultFilterDept`

**返回信息**:
- 是否有筛选
- 筛选部门ID
- 是否包含子部门
- 可选部门ID列表
- 根部门ID（用于前端构建部门树）

### 数据模型

**数据库表**: `sys_user`

**核心字段**:
```sql
- id: 用户ID
- username: 用户名（唯一）
- password: 密码（加密）
- real_name: 真实姓名
- email: 邮箱
- phone: 手机号
- dept_id: 部门ID
- status: 状态（0-禁用，1-启用）
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

**关联表**:
- `sys_user_role` - 用户角色关联表

---

## 部门管理

### 功能概述

管理组织架构部门信息，支持树形结构的部门层级。

### 部门层级

```
一级：总公司 (level=1, parent_id=0)
  └─ 二级：总部门/分部门 (level=2)
      └─ 三级：具体部门 (level=3)
```

### 核心功能

#### 1. 部门树

**功能说明**: 获取完整的部门树结构

**API**: `GET /dept/tree`

#### 2. 创建部门

**功能说明**: 创建新部门

**字段**:
- 部门名称
- 父部门ID
- 部门编号（code，6位数字字母混合）
- 负责人ID
- 排序

**自动生成**:
- 层级（level）
- 完整部门编号（full_code）

**API**: `POST /dept`

#### 3. 编辑部门

**功能说明**: 更新部门信息

**API**: `PUT /dept`

#### 4. 删除部门

**功能说明**: 删除部门

**规则**:
- 有子部门的部门不能删除
- 有用户的部门不能删除

**API**: `DELETE /dept/{id}`

### 数据模型

**数据库表**: `sys_dept`

**核心字段**:
```sql
- id: 部门ID
- name: 部门名称
- parent_id: 父部门ID（0表示根节点）
- code: 部门编号（唯一）
- level: 部门层级（1-一级，2-二级，3-三级）
- full_code: 完整部门编号（如：COMPAN-GENDEP-GENCON）
- leader_id: 负责人ID
- sort: 排序
- status: 状态（0-禁用，1-启用）
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

---

## 角色管理

### 功能概述

管理系统角色，包括角色的创建、编辑、删除和权限配置。

### 角色类型

| 角色类型 | 代码 | 说明 | 权限范围 |
|---------|------|------|----------|
| 系统管理员 | SYSTEM_ADMIN | 拥有系统所有权限 | 所有功能和数据 |
| 总消保管理岗 | GENERAL_MGMT | 管理所有部门和用户 | 所有部门和用户，所有角色 |
| 分消保管理岗 | BRANCH_MGMT | 管理二级机构用户 | 所属二级机构，部分角色 |
| 总消保用户 | GENERAL_USER | 总消保部普通用户 | 基础功能 |
| 分消保用户 | BRANCH_USER | 分消保部普通用户 | 基础功能 |
| 自定义角色 | CUSTOM | 自定义角色 | 根据分配的权限 |

### 核心功能

#### 1. 角色列表

**功能说明**: 查询所有角色

**API**: `GET /role/list`

#### 2. 创建角色

**功能说明**: 创建新角色

**字段**:
- 角色编码（唯一）
- 角色名称
- 角色类型
- 描述
- 可管理部门范围（dept_scope）

**API**: `POST /role`

#### 3. 编辑角色

**功能说明**: 更新角色信息

**API**: `PUT /role`

#### 4. 删除角色

**功能说明**: 删除角色

**规则**:
- 有用户的角色不能删除
- 系统内置角色不能删除

**API**: `DELETE /role/{id}`

#### 5. 配置权限

**功能说明**: 为角色分配菜单权限

**API**: `POST /menu/assign`

**参数**:
- `roleId`: 角色ID
- `menuIds`: 菜单ID列表

#### 6. 查询角色权限

**功能说明**: 查询角色拥有的菜单权限

**API**: `GET /menu/role/{roleId}`

### 数据模型

**数据库表**: `sys_role`

**核心字段**:
```sql
- id: 角色ID
- code: 角色编码（唯一）
- name: 角色名称
- role_type: 角色类型
- description: 描述
- dept_scope: 可管理部门范围（逗号分隔的部门ID）
- status: 状态（0-禁用，1-启用）
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

**关联表**:
- `sys_user_role` - 用户角色关联表
- `sys_role_menu` - 角色菜单关联表

---

## 菜单管理

### 功能概述

管理系统菜单结构，配置前端路由和权限控制。

### 菜单类型

| 菜单类型 | 代码 | 说明 | 示例 |
|---------|------|------|------|
| 菜单 | MENU | 前端页面菜单 | 素材管理 |
| 按钮 | BUTTON | 页面内按钮权限 | 上传素材、删除素材 |

### 菜单结构

```
一级菜单
  ├─ 二级菜单（页面）
  │   ├─ 三级菜单（可选）
  │   └─ 按钮权限
  └─ 按钮权限
```

### 系统菜单

#### 一级菜单

1. **素材管理** (`/asset`)
   - 素材列表

2. **流程管理** (`/workflow`)
   - 流程列表
   - 流程设计

3. **审批管理** (`/approval`)
   - 待我审批
   - 我发起的
   - 草稿箱
   - 审批工单（素材录入审批）

4. **系统管理** (`/system`)
   - 用户管理
   - 角色管理
   - 部门管理
   - 菜单管理

### 核心功能

#### 1. 菜单树

**功能说明**: 获取完整的菜单树结构

**API**: `GET /menu/tree`

#### 2. 获取当前用户菜单

**功能说明**: 获取当前用户有权限访问的菜单

**逻辑**:
1. 获取当前用户的所有角色
2. 获取角色的所有菜单权限
3. 构建菜单树
4. 过滤掉无权限的菜单

**API**: `GET /menu/current`

#### 3. 创建菜单

**功能说明**: 创建新菜单

**字段**:
- 菜单名称
- 父菜单ID
- 菜单类型（MENU/BUTTON）
- 路由路径
- 组件路径
- 图标
- 排序

**API**: `POST /menu`

#### 4. 编辑菜单

**功能说明**: 更新菜单信息

**API**: `PUT /menu`

#### 5. 删除菜单

**功能说明**: 删除菜单

**规则**:
- 有子菜单的菜单不能删除
- 删除菜单时同步删除角色关联

**API**: `DELETE /menu/{id}`

#### 6. 分配菜单权限

**功能说明**: 为角色分配菜单权限

**API**: `POST /menu/assign`

**参数**:
- `roleId`: 角色ID
- `menuIds`: 菜单ID列表

### 数据模型

**数据库表**:
- `sys_menu` - 菜单表
- `sys_role_menu` - 角色菜单关联表

**核心字段**:

**sys_menu**:
```sql
- id: 菜单ID
- parent_id: 父菜单ID（0表示根菜单）
- name: 菜单名称
- type: 类型（MENU/BUTTON）
- path: 路由路径
- component: 组件路径
- icon: 图标
- sort: 排序
- status: 状态（0-禁用，1-启用）
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

**sys_role_menu**:
```sql
- id: 关联ID
- role_id: 角色ID
- menu_id: 菜单ID
```

---

## 标签管理

### 功能概述

管理素材标签，支持标签分类和多维度标记。

### 标签分类

| 分类 | 说明 | 示例 |
|------|------|------|
| 内容类型 | 素材内容分类 | 宣传片、教程、案例 |
| 行业分类 | 适用行业 | 金融、教育、医疗 |
| 主题标签 | 内容主题 | 创新、合规、服务 |

### 核心功能

#### 1. 创建标签

**功能说明**: 创建新标签

**字段**:
- 标签名称
- 标签分类（可选）

**API**: `POST /tag`

#### 2. 获取所有标签

**功能说明**: 查询所有标签

**API**: `GET /tag/list`

#### 3. 按分类获取标签

**功能说明**: 查询指定分类的标签

**API**: `GET /tag/list/{category}`

#### 4. 删除标签

**功能说明**: 删除标签

**规则**:
- 被素材使用的标签不能删除

**API**: `DELETE /tag/{id}`

### 数据模型

**数据库表**: `tag`

**核心字段**:
```sql
- id: 标签ID
- name: 标签名称
- category: 标签分类
- create_time: 创建时间
- update_time: 更新时间
- deleted: 删除标记
```

---

## 日志管理

### 功能概述

记录用户操作日志和素材使用日志，提供审计和统计功能。

### 日志类型

| 日志类型 | 说明 | 记录内容 |
|---------|------|----------|
| 使用日志 | 素材下载和使用 | 素材、用户、时间、IP等 |
| 操作日志 | 用户操作行为 | 操作类型、操作对象、时间等 |

### 使用日志

#### 记录信息

**触发时机**: 用户下载素材时自动记录

**记录内容**:
- 素材ID
- 用户ID
- 用户姓名
- 部门名称
- 操作类型（下载）
- IP地址
- 使用说明
- 发布渠道
- 使用时间

#### 查询使用日志

**功能说明**: 查看素材的使用记录

**展示位置**:
- 素材预览弹窗
- 素材详情页

**筛选条件**:
- 素材ID
- 用户ID
- 时间范围

**API**: 通过素材详情接口返回

### 数据模型

**数据库表**: `usage_log`

**核心字段**:
```sql
- id: 日志ID
- asset_id: 素材ID
- user_id: 用户ID
- user_name: 用户姓名
- dept_name: 部门名称
- action: 操作类型（DOWNLOAD等）
- ip: IP地址
- usage_description: 使用说明
- usage_publish_channel: 发布渠道
- create_time: 使用时间
```

---

## 工作流引擎功能

### 功能概述

系统采用自研工作流引擎，支持"层级顺序 + 层内并行"的审批模式，并提供子流程、退回、追回等高级功能。

### 核心特性

#### 1. 层级顺序 + 层内并行

**层级顺序（串行）**:
- 阶段之间严格按顺序执行
- 第 N 阶段完成才能进入第 N+1 阶段
- 阶段自动跳过（无审批人时）

**层内并行**:
- 同阶段所有审批人同时收到任务
- 不分先后顺序
- 支持或签/会签

#### 2. 层内规则

**或签（OR）**:
- 任一人审批通过即通过
- 第一个审批的人为"首审批人"
- 其他人任务自动取消

**会签（AND）**:
- 所有人必须审批通过
- 任务创建时标记"首审批人"
- 所有人通过后阶段完成

#### 3. 首审批人机制

**首审批人职责**:
- 选择下一阶段审批人
- 触发子流程（如果配置）
- 查看审批进度

**首审批人识别**:
- 或签：第一个审批的人
- 会签：任务创建时标记的人（`is_first_approver=1`）

#### 4. 子流程支持

**配置方式**:
- 在审批人级别配置子流程ID
- 一个审批人可以触发一个子流程

**触发方式**:
- 由首审批人手动触发
- 选择子流程审批人

**执行特点**:
- 独立运行，不阻塞主流程
- 并行执行
- 主流程和子流程都完成才算最终完成

**实例关系**:
- 子流程实例记录父实例ID（`parent_instance_id`）
- 子流程实例记录父任务ID（`parent_task_id`）
- 支持多层嵌套

#### 5. 审批操作

**审批通过**:
- 检查阶段是否完成
- 如果完成，进入下一阶段
- 如果是最后阶段，完成审批
- 主流程完成后等待子流程

**审批驳回**:
- 整个实例状态变为 REJECTED
- 取消所有待审批任务
- 通知发起人

**退回上一级**:
- 取消当前阶段任务
- 重新创建上一阶段任务
- 更新实例状态
- 通知上一阶段审批人

**追回工单**:
- 发起人可追回 PENDING 或 MAIN_COMPLETED 状态的实例
- 取消所有待审批任务
- 实例状态变为 RETURNED
- 通知相关审批人

#### 6. 审批进度追踪

**进度展示**:
- 流程图形式
- 各阶段状态
- 各审批人审批结果
- 审批意见和时间
- 子流程进度

**状态流转**:
```
PENDING（待审批）
  ↓
MAIN_COMPLETED（主流程完成，等待子流程）
  ↓
APPROVED（已批准）

PENDING → REJECTED（被驳回）
PENDING → RETURNED（被退回）
```

### 技术实现

**核心服务**:
- `WorkflowEngineService` - 工作流引擎核心服务
- `ApproverSelectionService` - 审批人选择服务
- `ApprovalService` - 审批服务

**关键方法**:
- `createInstance()` - 创建审批实例
- `completeTask()` - 完成审批任务
- `selectFirstStageApprovers()` - 选择首阶段审批人
- `checkAndMoveToNextStage()` - 检查并进入下一阶段
- `startSubProcessesForStage()` - 启动子流程
- `areAllSubWorkflowsComplete()` - 检查子流程是否完成
- `withdrawInstance()` - 追回工单
- `returnTask()` - 退回上一级
- `restartSubWorkflow()` - 重新发起子流程

---

## 系统配置

### 服务端配置

**配置文件**: `xuanjiao-start/src/main/resources/application.yml`

**核心配置**:

**服务器配置**:
```yaml
server:
  port: 8080
  servlet:
    context-path: /api
```

**数据库配置**:
```yaml
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/xuanjiao_s
    username: root
    password: 123456
```

**文件上传配置**:
```yaml
file:
  upload-path: D:/xuanjiao/uploads/
  max-size: 100MB
```

**JWT配置**:
```yaml
jwt:
  secret: xuanjiao-secret-key
  expiration: 86400000  # 24小时
```

### 前端配置

**配置文件**: `xuanjiao-frontend/vite.config.ts`

**开发服务器**:
```typescript
server: {
  port: 3000
}
```

**API代理**:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

### 数据库配置

**初始化脚本**: `xuanjiao-backend/sql/init_all.sql`

**执行命令**:
```bash
mysql -u root -p123456 < xuanjiao-backend/sql/init_all.sql
```

**迁移脚本**:
按顺序执行以下脚本：
- init_17_sub_workflow_refactor.sql
- init_18_add_sub_workflow_approver_ids.sql
- init_19_add_sub_workflow_approver_ids_to_instance.sql
- init_20_add_material_approval_menu.sql
- init_21_add_usage_menu.sql
- init_22_extend_asset_for_usage.sql
- init_23_extend_usage_log.sql
- init_24_refactor_to_intermediate_table.sql
- init_25_refactor_menu_and_draft.sql

### 访问地址

**前端地址**: http://localhost:3000

**后端API**: http://localhost:8080/api

**API文档**: http://localhost:8080/api/doc.html

**默认账号**: admin / 123456

---

## 附录

### 术语表

| 术语 | 解释 |
|------|------|
| 素材 | 系统中管理的各类媒体文件资源 |
| 审批流程 | 可配置的审批工作流定义 |
| 审批实例 | 审批流程的一次执行 |
| 审批任务 | 分配给审批人的待办任务 |
| 审批阶段 | 审批流程中的一个层级 |
| 首审批人 | 第一个审批的人，负责选择下阶段审批人 |
| 或签 | 任一人审批通过即可 |
| 会签 | 所有人都必须审批通过 |
| 子流程 | 独立的审批流程，由主流程触发 |
| 使用申请 | 用户申请使用素材的申请单 |
| 素材录入 | 用户上传素材并申请入库 |
| 中间表 | 用于实现多对多关系的关联表 |
| 逻辑删除 | 不物理删除数据，只标记为已删除 |

### 业务流程图

#### 素材录入流程

```
1. 用户上传素材
   ↓
2. 创建素材录入申请
   ↓
3. 选择审批流程（素材录入类型）
   ↓
4. 选择首阶段审批人
   ↓
5. 提交审批
   ↓
6. 审批人依次审批
   ↓
7a. 审批通过 → 素材状态变为 APPROVED
7b. 审批驳回 → 素材状态变为 REJECTED
7c. 审批退回 → 返回上一阶段
```

#### 素材使用流程

```
1. 用户选择素材
   ↓
2. 创建使用申请（可多选）
   ↓
3. 配置每素材的使用信息
   ↓
4. 选择审批流程（素材使用类型）
   ↓
5. 选择首阶段审批人
   ↓
6. 提交审批
   ↓
7. 审批人依次审批
   ↓
8a. 审批通过 → 获得下载权限
8b. 审批驳回 → 申请失败
8c. 审批退回 → 返回上一阶段
   ↓
9. 下载素材
   ↓
10. 记录使用日志
```

### 状态转换图

#### 素材状态转换

```
DRAFT（草稿）
  ↓ 提交审批
PENDING（待审）
  ↓
  ├─→ APPROVED（可用）- 审批通过
  ├─→ REJECTED（驳回）- 审批驳回
  └─→ RETURNED（已退回）- 退回修改
       ↓ 重新提交
    PENDING（待审）
```

#### 审批实例状态转换

```
PENDING（待审批）
  ↓
  ├─→ MAIN_COMPLETED（主流程完成）- 等待子流程
  │     ↓
  │     └─→ APPROVED（已批准）- 子流程完成
  │
  ├─→ REJECTED（已驳回）- 任一阶段驳回
  │
  └─→ RETURNED（已退回）- 发起人追回
```

---

**文档结束**

本文档详细说明了宣传教育平台的所有业务功能点，包括核心业务模块、工作流引擎、权限管理等方面。如需了解更多技术细节，请参考：
- `CLAUDE.md` - 项目开发指南
- `REQUIREMENTS.md` - 产品需求文档
- `WORKFLOW_REFACTOR_SUMMARY.md` - 工作流架构文档
