<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuanjiao.infrastructure.user.UserMapper">

    <!-- 结果映射：column是数据库字段（下划线），property是Java属性（驼峰） -->
    <resultMap id="BaseResultMap" type="com.xuanjiao.infrastructure.dataobject.UserDO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="dept_id" property="deptId" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 基础字段列表（数据库字段，下划线命名） -->
    <sql id="Base_Column_List">
        id, username, password, real_name, email, phone, dept_id, role_id,
        status, create_time, update_time, deleted
    </sql>

    <!-- 结果映射：用户+部门+角色（JOIN查询结果） -->
    <resultMap id="UserWithDeptRoleResultMap" type="com.xuanjiao.infrastructure.dataobject.UserWithDeptRoleDO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <!-- 关联字段 -->
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_type" property="roleType" jdbcType="VARCHAR"/>
        <result column="dept_id" property="deptId" jdbcType="BIGINT"/>
        <result column="dept_name" property="deptName" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 根据主键查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            id = #{id}
            AND deleted = 0
        </where>
        LIMIT 1
    </select>

    <!-- 根据用户名查询单个用户 -->
    <select id="selectOneByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            username = #{username}
            AND deleted = 0
        </where>
        LIMIT 1
    </select>

    <!-- 条件查询用户列表 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        <where>
            <if test="deleted != null">
                deleted = #{deleted}
            </if>
            <if test="deleted == null">
                deleted = 0
            </if>
            <if test="roleId != null">
                AND role_id = #{roleId}
            </if>
            <if test="roleIds != null and roleIds.size() > 0">
                AND role_id IN
                <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                    #{roleId}
                </foreach>
            </if>
            <if test="deptId != null">
                AND dept_id = #{deptId}
            </if>
            <if test="deptIds != null and deptIds.size() > 0">
                AND dept_id IN
                <foreach collection="deptIds" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="usernameKeyword != null and usernameKeyword != ''">
                AND username LIKE CONCAT('%', #{usernameKeyword}, '%')
            </if>
            <if test="realNameKeyword != null and realNameKeyword != ''">
                AND real_name LIKE CONCAT('%', #{realNameKeyword}, '%')
            </if>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%') OR real_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="userIds != null and userIds.size() > 0">
                AND id IN
                <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
        </where>
        ORDER BY id
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.xuanjiao.infrastructure.dataobject.UserDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username, password, real_name, email, phone, dept_id, role_id,
            status, create_time, update_time, deleted
        ) VALUES (
            #{username}, #{password}, #{realName}, #{email}, #{phone}, #{deptId}, #{roleId},
            #{status}, #{createTime}, #{updateTime}, #{deleted}
        )
    </insert>

    <!-- 根据主键更新用户 -->
    <update id="updateById" parameterType="com.xuanjiao.infrastructure.dataobject.UserDO">
        UPDATE sys_user
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="realName != null and realName != ''">real_name = #{realName},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <!-- 根据主键删除用户（逻辑删除） -->
    <update id="deleteById">
        UPDATE sys_user
        <set>
            deleted = 1
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <!-- 根据角色ID查询用户ID列表 -->
    <select id="selectUserIdsByRoleId" resultType="java.lang.Long">
        SELECT id
        FROM sys_user
        <where>
            role_id = #{roleId}
            AND status = 1
            AND deleted = 0
        </where>
    </select>

    <!-- 查询用户列表（JOIN 部门和角色，避免 N+1） -->
    <select id="selectListWithDeptRole" resultMap="UserWithDeptRoleResultMap">
        SELECT
            u.id, u.username, u.real_name, u.email, u.phone, u.status, u.create_time,
            u.role_id, r.name AS role_name, r.role_type,
            u.dept_id, d.name AS dept_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        LEFT JOIN sys_dept d ON u.dept_id = d.id
        <where>
            u.deleted = 0
            <if test="roleId != null">
                AND u.role_id = #{roleId}
            </if>
            <if test="roleIds != null and roleIds.size() > 0">
                AND u.role_id IN
                <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                    #{roleId}
                </foreach>
            </if>
            <if test="deptId != null">
                AND u.dept_id = #{deptId}
            </if>
            <if test="deptIds != null and deptIds.size() > 0">
                AND u.dept_id IN
                <foreach collection="deptIds" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <if test="subDeptIds != null and subDeptIds.size() > 0">
                AND u.dept_id IN
                <foreach collection="subDeptIds" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <if test="status != null">
                AND u.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (u.username LIKE CONCAT('%', #{keyword}, '%')
                     OR u.real_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="userIds != null and userIds.size() > 0">
                AND u.id IN
                <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
        </where>
        ORDER BY u.id
    </select>

    <!-- 分页查询用户列表（JOIN 部门和角色） -->
    <select id="selectPageWithDeptRole" resultMap="UserWithDeptRoleResultMap">
        SELECT
            u.id, u.username, u.real_name, u.email, u.phone, u.status, u.create_time,
            u.role_id, r.name AS role_name, r.role_type,
            u.dept_id, d.name AS dept_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        LEFT JOIN sys_dept d ON u.dept_id = d.id
        <where>
            u.deleted = 0
            <if test="query.roleId != null">
                AND u.role_id = #{query.roleId}
            </if>
            <if test="query.roleIds != null and query.roleIds.size() > 0">
                AND u.role_id IN
                <foreach collection="query.roleIds" item="roleId" open="(" separator="," close=")">
                    #{roleId}
                </foreach>
            </if>
            <if test="query.deptId != null">
                AND u.dept_id = #{query.deptId}
            </if>
            <if test="query.deptIds != null and query.deptIds.size() > 0">
                AND u.dept_id IN
                <foreach collection="query.deptIds" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <if test="query.subDeptIds != null and query.subDeptIds.size() > 0">
                AND u.dept_id IN
                <foreach collection="query.subDeptIds" item="deptId" open="(" separator="," close=")">
                    #{deptId}
                </foreach>
            </if>
            <if test="query.status != null">
                AND u.status = #{query.status}
            </if>
            <if test="query.keyword != null and query.keyword != ''">
                AND (u.username LIKE CONCAT('%', #{query.keyword}, '%')
                     OR u.real_name LIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
            <if test="query.userIds != null and query.userIds.size() > 0">
                AND u.id IN
                <foreach collection="query.userIds" item="userId" open="(" separator="," close=")">
                    #{userId}
                </foreach>
            </if>
        </where>
        ORDER BY u.id
    </select>

</mapper>
