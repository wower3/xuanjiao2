<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuanjiao.infrastructure.deletion.AssetDeletionApplicationMapper">

    <resultMap id="BaseResultMap" type="com.xuanjiao.infrastructure.dataobject.AssetDeletionApplicationDO">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="applicant_id" property="applicantId" jdbcType="BIGINT"/>
        <result column="dept_id" property="deptId" jdbcType="BIGINT"/>
        <result column="workflow_id" property="workflowId" jdbcType="BIGINT"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="delete_reason" property="deleteReason" jdbcType="VARCHAR"/>
        <result column="attachment_path" property="attachmentPath" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, applicant_id, dept_id, workflow_id, status, delete_reason,
        attachment_path, create_time, update_time, deleted
    </sql>

    <!-- ==================== 基础CRUD方法 ==================== -->

    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT <include refid="Base_Column_List"/>
        FROM asset_deletion_application
        WHERE id = #{id}
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectOne" resultMap="BaseResultMap" parameterType="com.xuanjiao.infrastructure.deletion.AssetDeletionApplicationQuery">
        SELECT <include refid="Base_Column_List"/>
        FROM asset_deletion_application
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="title != null and title != ''">
                AND title = #{title}
            </if>
            <if test="applicantId != null">
                AND applicant_id = #{applicantId}
            </if>
            <if test="deptId != null">
                AND dept_id = #{deptId}
            </if>
            <if test="workflowId != null">
                AND workflow_id = #{workflowId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="deleted != null">
                AND deleted = #{deleted}
            </if>
            <if test="applicantIds != null and applicantIds.size() > 0">
                AND applicant_id IN
                <foreach collection="applicantIds" item="applicantId" open="(" separator="," close=")">
                    #{applicantId}
                </foreach>
            </if>
            <if test="statusIn != null and statusIn.size() > 0">
                AND status IN
                <foreach collection="statusIn" item="s" open="(" separator="," close=")">
                    #{s}
                </foreach>
            </if>
        </where>
        AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectList" resultMap="BaseResultMap" parameterType="com.xuanjiao.infrastructure.deletion.AssetDeletionApplicationQuery">
        SELECT <include refid="Base_Column_List"/>
        FROM asset_deletion_application
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="title != null and title != ''">
                AND title = #{title}
            </if>
            <if test="applicantId != null">
                AND applicant_id = #{applicantId}
            </if>
            <if test="deptId != null">
                AND dept_id = #{deptId}
            </if>
            <if test="workflowId != null">
                AND workflow_id = #{workflowId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="deleted != null">
                AND deleted = #{deleted}
            </if>
            <if test="applicantIds != null and applicantIds.size() > 0">
                AND applicant_id IN
                <foreach collection="applicantIds" item="applicantId" open="(" separator="," close=")">
                    #{applicantId}
                </foreach>
            </if>
            <if test="statusIn != null and statusIn.size() > 0">
                AND status IN
                <foreach collection="statusIn" item="s" open="(" separator="," close=")">
                    #{s}
                </foreach>
            </if>
        </where>
        AND deleted = 0
        <if test="orderByField != null and orderByField != ''">
            ORDER BY ${orderByField}
            <if test="orderByDirection != null and orderByDirection != ''">
                ${orderByDirection}
            </if>
        </if>
        <if test="orderByField == null or orderByField == ''">
            ORDER BY create_time DESC
        </if>
    </select>

    <select id="selectCount" resultType="java.lang.Long" parameterType="com.xuanjiao.infrastructure.deletion.AssetDeletionApplicationQuery">
        SELECT COUNT(*)
        FROM asset_deletion_application
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="title != null and title != ''">
                AND title = #{title}
            </if>
            <if test="applicantId != null">
                AND applicant_id = #{applicantId}
            </if>
            <if test="deptId != null">
                AND dept_id = #{deptId}
            </if>
            <if test="workflowId != null">
                AND workflow_id = #{workflowId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="deleted != null">
                AND deleted = #{deleted}
            </if>
            <if test="applicantIds != null and applicantIds.size() > 0">
                AND applicant_id IN
                <foreach collection="applicantIds" item="applicantId" open="(" separator="," close=")">
                    #{applicantId}
                </foreach>
            </if>
            <if test="statusIn != null and statusIn.size() > 0">
                AND status IN
                <foreach collection="statusIn" item="s" open="(" separator="," close=")">
                    #{s}
                </foreach>
            </if>
        </where>
        AND deleted = 0
    </select>

    <!-- MyBatis-Plus 分页查询 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM asset_deletion_application
        <where>
            <if test="query.id != null">
                AND id = #{query.id}
            </if>
            <if test="query.title != null and query.title != ''">
                AND title = #{query.title}
            </if>
            <if test="query.applicantId != null">
                AND applicant_id = #{query.applicantId}
            </if>
            <if test="query.deptId != null">
                AND dept_id = #{query.deptId}
            </if>
            <if test="query.workflowId != null">
                AND workflow_id = #{query.workflowId}
            </if>
            <if test="query.status != null and query.status != ''">
                AND status = #{query.status}
            </if>
            <if test="query.deleted != null">
                AND deleted = #{query.deleted}
            </if>
            <if test="query.applicantIds != null and query.applicantIds.size() > 0">
                AND applicant_id IN
                <foreach collection="query.applicantIds" item="applicantId" open="(" separator="," close=")">
                    #{applicantId}
                </foreach>
            </if>
            <if test="query.statusIn != null and query.statusIn.size() > 0">
                AND status IN
                <foreach collection="query.statusIn" item="s" open="(" separator="," close=")">
                    #{s}
                </foreach>
            </if>
        </where>
        AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.xuanjiao.infrastructure.dataobject.AssetDeletionApplicationDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO asset_deletion_application (title, applicant_id, dept_id, workflow_id, status,
                                               delete_reason, attachment_path)
        VALUES (#{title}, #{applicantId}, #{deptId}, #{workflowId}, #{status},
                #{deleteReason}, #{attachmentPath})
    </insert>

    <update id="updateById" parameterType="com.xuanjiao.infrastructure.dataobject.AssetDeletionApplicationDO">
        UPDATE asset_deletion_application
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="applicantId != null">
                applicant_id = #{applicantId},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="workflowId != null">
                workflow_id = #{workflowId},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="deleteReason != null">
                delete_reason = #{deleteReason},
            </if>
            <if test="attachmentPath != null">
                attachment_path = #{attachmentPath},
            </if>
        </set>
        WHERE id = #{id}
        AND deleted = 0
    </update>

    <delete id="deleteById">
        UPDATE asset_deletion_application SET deleted = 1 WHERE id = #{id}
    </delete>

    <delete id="delete" parameterType="com.xuanjiao.infrastructure.deletion.AssetDeletionApplicationQuery">
        UPDATE asset_deletion_application SET deleted = 1
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="applicantId != null">
                AND applicant_id = #{applicantId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </delete>

</mapper>
