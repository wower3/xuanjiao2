<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuanjiao.infrastructure.role.RoleMenuMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.xuanjiao.infrastructure.dataobject.RoleMenuDO">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="menu_id" property="menuId"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="Base_Column_List">
        id, role_id, menu_id
    </sql>

    <!-- Select List with Dynamic Conditions -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_role_menu
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="roleId != null">AND role_id = #{roleId}</if>
            <if test="menuId != null">AND menu_id = #{menuId}</if>
        </where>
    </select>

    <!-- Select Count -->
    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sys_role_menu
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="roleId != null">AND role_id = #{roleId}</if>
            <if test="menuId != null">AND menu_id = #{menuId}</if>
        </where>
    </select>

    <!-- Insert -->
    <insert id="insert" parameterType="com.xuanjiao.infrastructure.dataobject.RoleMenuDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role_menu (role_id, menu_id)
        VALUES (#{roleId}, #{menuId})
    </insert>

    <!-- Delete By RoleId (Hard Delete) -->
    <delete id="deleteByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <!-- Delete By Query (Hard Delete) -->
    <delete id="delete" parameterType="com.xuanjiao.infrastructure.role.RoleMenuQuery">
        DELETE FROM sys_role_menu
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="roleId != null">AND role_id = #{roleId}</if>
            <if test="menuId != null">AND menu_id = #{menuId}</if>
        </where>
    </delete>

</mapper>
